require "spec_helper"

RSpec.describe A3rt::Talk do
  it "has a version number" do
    expect(A3rt::Talk::VERSION).not_to be nil
  end

  it "does something useful" do
    expect(false).to eq(true)
  end
end
